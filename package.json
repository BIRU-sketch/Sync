{
  "name": "voice-companion",
  "version": "0.0.0",
  "private": true,
  "description": "Low-latency desktop voice companion for developers, built on Voxide.",
  "main": "dist-electron/main.js",
  "scripts": {
    "build:electron": "tsc -p tsconfig.json",
    "dev:electron": "tsc -p tsconfig.json -w",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
